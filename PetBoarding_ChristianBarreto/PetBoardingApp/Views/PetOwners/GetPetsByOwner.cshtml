@model PetBoardingApp.Models.PetOwner

@{
    ViewBag.Title = "Pets List";
}

<h2>Pets for this Owner</h2>

<ul>

        @foreach (var pet in Model.Pets)
        {
            <li>@pet.Name</li>
        }
</ul>

